{
  "name": "fx-dashboard",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:web": "npm run dev --workspace=apps/web"
  },
  "dependencies": {
    "axios": "^1.14.0",
    "recharts": "^3.8.1"
  },
  "devDependencies": {
    "@types/recharts": "^1.8.29"
  }
}
